export enum WebSocketActions {
  ADMIN = "admin",
  DRAW = "draw",
  CLIENT_COUNT_UPDATE = "clientCountUpdate",
  COUNTDOWN = "countdown",
  RESULT = "result",
}

export enum WebSocketEvents {
  MESSAGE = "message",
  CLOSE = "close",
}
